#!/usr/bin/env bash
set -euo pipefail

# ensure python can import the `pylib` package located at "$HOME/.local/pylib"
export PYTHONPATH="$HOME/.local${PYTHONPATH:+:$PYTHONPATH}"

# allow running addhost via `addhost` command in the shell
exec python -m pylib addhost "$@"
